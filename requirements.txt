# Exquisite Stories pipeline — Python 3.11 venv (see SETUP.md).
# Pinned to majors known to ship wheels for Python 3.11 on macOS (arm64) and Linux.

# --- core image / array ---
# The image pipeline is OpenCV crop geometry only; hard cases escalate to the local `claude` CLI
# (no Python ML deps — no rembg/onnxruntime/torch/SAM2).
numpy>=1.26,<2.2
opencv-python>=4.9,<5
pillow>=10.2

# --- audio: transcription + alignment + io ---
faster-whisper>=1.0      # PRIMARY backend: word-level timestamps, stable API (CTranslate2 Whisper)
whisperx>=3.1            # fallback transcription + wav2vec align (faster-whisper preferred over it)
soundfile>=0.12
librosa>=0.10
pydub>=0.25

# --- chapter text reading (optional automated fallback; default uses the Claude orchestrator) ---
anthropic>=0.40

# --- reviewer web app ---
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9  # form/file uploads from the reviewer canvas

# --- misc ---
rapidfuzz>=3.6           # fuzzy alignment of chapter text to transcript words
tqdm>=4.66
